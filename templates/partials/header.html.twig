<nav class="navbar navbar-dark bg-primary container-fluid d-flex">
    <div class="mr-auto d-flex">
        <div class="nav-item">
            <a class="navbar-brand color-whi" href="{{ url('homepage') }}">SymBnB</a>
        </div>
        <div class="nav-item">
            <a class="nav-link active color-whi" href="{{ url('homepage') }}">Accueil
            </a>
        </div>
        <div class="nav-item">
            <a class="nav-link active color-whi" href="{{ url('ads_index') }}">Nos annonces
            </a>
        </div>
    </div>
    <ul class="ml-auto">
        {% if app.user %}
            <a class="nav-link dropdown-toggle color-whi" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Mon Profile
            </a>
            <div class="dropdown-menu pr-5 r" aria-labelledby="dropdownMenuLink">
                <a class="dropdown-item" href="{{ url('ads_create') }}">créer une annonce</a>
                <a class="dropdown-item" href="{{ path('account_index') }}">Mes annonces</a>
                <a class="dropdown-item" href="{{ path('account_bookings') }}">Mes réservation</a>
                <a class="dropdown-item" href="{{ path('profile') }}">Modifier mes informations</a>
                <a class="dropdown-item" href="{{ path('password') }}">Modifier mon mots de passe </a>
                <a href="{{ path('account_logout') }}" class="nav-link">Déconnexion</a>
            </div>
        {% else %}
            <div class="d-flex">
                <div class="nav-bar pr-5">
                    <a href="{{ path('account_login') }}" class="nav-link color-whi">Connexion</a>
                </div>
                <div class="nav-bar">
                    <a href="{{ path('accounte_register') }}" class="nav-link color-whi">Incription</a>
                </div>
            </div>
        {% endif %}
    </ul>
</nav>

{% for label, messages in app.flashes %}
    <div class="alert-{{ label }}">
        {%  for message in messages %}
            <p>{{ message | raw }}</p>
        {%  endfor %}
    </div>
{% endfor %}