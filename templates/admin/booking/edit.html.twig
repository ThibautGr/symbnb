{% extends 'admin/base.html.twig' %}
{% form_theme form _self %}
{% block title %}Administration de la réservation n°{{ booking.id }}{% endblock %}

{% block body %}
    <div class="container-fluid text-center">
        <div class="row justify-content-center">
            <div class="col-5">
                <div class="card-head">
                    <h2 class="my-5">Gestion de la réservation : n°{{ booking.id }}</h2>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                    <div class="card bg-light">
                        <div class="row">
                            <div class="col">
                                {{ form_row(form.startDate) }}
                            </div>
                            <div class="col">
                                {{ form_row(form.endTime) }}
                            </div>
                        </div>
                        {{ form_widget(form) }}
                    </div>
                    <button type="submit" class="btn btn-primary">Validé</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block _ad_images_widget %}
    {{ form_widget(form) }}
{% endblock %}
