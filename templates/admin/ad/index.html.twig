{% extends 'admin/base.html.twig' %}

{% block title %}Administration des annonces!{% endblock %}

{% block body %}

    <div class="container-fluid">
        <h1 class="my-5">Gestion des annonces</h1>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>id</th>
                    <th>Titre</th>
                    <th>Réservation</th>
                    <th>note</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
            {% for ad in ads %}
                <tr>
                    <td>{{ ad.id }}</td>
                    <td>{{ ad.title }}</td>
                    <td>{{ ad.author.fullname }}</td>
                    <td>{{ ad.author.bookings | length }}</td>
                    <td>{{ ad.avgRattings }}</td>
                    <td>
                        <a href="{{ path('admin_ad_edit',{'id': ad.id}) }}" class="btn btn-primary"><i class="fas fa-edit"></i></a>
                        <a href="{{ path('admin_ad_delete',{'id':ad.id}) }}" class="btn btn-primary"><i class="fas fa-trash"></i></a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        {% include 'admin/partials/_pagination.html.twig' with {'route':'admin_ads_index'}  %}
    </div>

{% endblock %}
