{%  extends 'base.html.twig' %}

{% block title %} Mettre a jour mon mot de passe. {% endblock %}

{% block body %}
    <div class="container">
        <h1>Mettre a jour mon mot de passe. </h1>
        {{ form_start(form) }}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn-primary btn">
            Enregistrer les modifications
        </button>
        {{ form_end(form) }}
    </div>

{% endblock %}